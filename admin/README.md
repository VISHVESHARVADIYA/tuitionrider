# Admin Folder

The admin dashboard UI is implemented inside the React client at `/admin/login` and `/admin/dashboard`.

This folder is kept to match the requested deployment-ready project structure.
